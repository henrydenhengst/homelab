# Je hoofd-dashboard
{{ duckdns_domain }}.duckdns.org {
    reverse_proxy localhost:5005
}

# Optioneel: Portainer op een subdomein
portainer.{{ duckdns_domain }}.duckdns.org {
    reverse_proxy localhost:9443 {
        transport http {
            tls_insecure_skip_verify
        }
    }
}
