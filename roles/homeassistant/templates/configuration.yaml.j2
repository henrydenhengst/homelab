# Basis configuratie
default_config:

# Frontend (Dashboard)
frontend:

# Recorder instellen voor PostgreSQL
recorder:
  db_url: postgresql://{{ vault_postgres_user }}:{{ vault_postgres_password }}@localhost/homeassistant
  purge_keep_days: 30
  commit_interval: 10

# Ontdekking van mobiele apps
mobile_app:

# Notificaties
discovery:
